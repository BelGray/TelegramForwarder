# 🛰️ Telegram Smart Broadcaster (Rotator) v2.1
### Профессиональный программный комплекс для автоматизированного управления контентом и рекламного вещания в среде Telegram.

---

## 1. ВВЕДЕНИЕ И НАЗНАЧЕНИЕ
Данное программное обеспечение представляет собой высокопроизводительный Backend-сервис (Userbot), построенный на асинхронной архитектуре. В отличие от стандартных систем пересылки, работающих по событийной модели, данный комплекс реализует логику **Broadcaster (Вещателя)**.

Система предназначена для поддержания постоянной активности в сетке целевых чатов путем цикличной ротации актуальных предложений из канала-донора.

---

## 2. ТЕХНИЧЕСКИЙ СТЕК
*   **Язык разработки:** Python 3.10+
*   **Ядро системы:** Pyrogram (библиотека для взаимодействия с MTProto API).
*   **База данных:** MySQL 8.0+ (обеспечение персистентности данных и хранения состояний).
*   **Драйвер БД:** aiomysql (асинхронное взаимодействие с СУБД).
*   **Среда развертывания:** Linux (Debian/Ubuntu), полная поддержка Docker-контейнеризации.

---

## 3. АРХИТЕКТУРА И ЛОГИКА РАБОТЫ

### 3.1. Алгоритм «Умного Ротатора»
Система работает в режиме бесконечного цикла (Polling), независимо обрабатывая каждый целевой чат. 

1.  **Валидация таймера:** Для каждого чата из БД извлекается метка `last_sent_at`. Система сравнивает текущее время в формате UTC с временем последней отправки.
2.  **Динамический срез контента:** Бот обращается к источнику и загружает последние 20 сообщений. Это гарантирует **автоматическую актуальность**: если товар удален из канала-донора менеджером, бот мгновенно исключает его из ротации.
3.  **Интеллектуальная фильтрация:** Система анализирует структуру каждого сообщения. В ротацию попадают только объекты, содержащие `reply_markup` (Inline-кнопки). Текстовый шум, опросы и сервисные сообщения отсекаются программно.
4.  **Пакетная генерация:** На основе параметра `batch_size` система формирует уникальную пачку из N случайных актуальных постов для конкретной итерации.

### 3.2. Система ротации и отказоустойчивости
Для минимизации рисков блокировки аккаунтов внедрена многоуровневая система защиты:
*   **Account Switching:** При каждой попытке отправки система выбирает свободный аккаунт из пула.
*   **FloodWait Handling:** При получении временного ограничения от Telegram, аккаунт автоматически помечается в БД статусом `flood_wait` и выводится из ротации на 30 минут.
*   **Dead Session Detection:** Аккаунты с аннулированными сессиями или банами помечаются статусом `banned` и исключаются из работы до вмешательства администратора.

---

## 4. РЕЖИМЫ ТРАНСЛЯЦИИ (FORWARD VS COPY)
В системе реализовано два фундаментально разных метода доставки контента, настраиваемых индивидуально для каждого чата:

| Характеристика | Mode 0 (Forward) | Mode 1 (Copy) |
| :--- | :--- | :--- |
| **Вид поста** | Репост с указанием автора | Нативный пост от имени аккаунта |
| **Кнопки** | Сохраняются и работают | Удаляются (ограничение Telegram) |
| **Проходимость** | Может блокироваться в закрытых чатах | Проходит в 100% чатов |
| **Рекомендация** | Для чатов с разрешенным Forward | Для чатов с жесткой модерацией |

---

## 5. УСТАНОВКА И РАЗВЕРТЫВАНИЕ

### 5.1. Подготовка базы данных
1. Создайте базу данных `telegram_forwarder`.
2. Импортируйте структуру из файла `database.sql`.
3. Убедитесь, что сервер MySQL доступен для подключения.

### 5.2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 5.3. Конфигурация окружения (.env)
Создайте файл `.env` и заполните следующие параметры:
*   `API_ID` / `API_HASH`: Данные вашего приложения Telegram.
*   `DB_HOST` / `DB_USER` / `DB_PASS`: Доступы к MySQL.
*   `ADMIN_ID`: Ваш числовой Telegram ID (для доступа к командам).

---

## 6. ИНСТРУКЦИЯ ПО ЭКСПЛУАТАЦИИ (CLI)

Управление системой осуществляется через отправку команд любому из запущенных аккаунтов-ботов.

### 6.1. Первичная авторизация
Запустите `python add_account.py`. Скрипт проведет вас через процесс авторизации. Сессия будет зашифрована и сохранена в БД. Повторите для всех имеющихся аккаунтов.

### 6.2. Команды управления чатами
*   `/add_source @link` — Установить канал-витрину.
*   `/add_dest @link [интервал] [пачка]` — Добавить чат-получатель.
    *   *Пример:* `/add_dest @my_chat 30 2` (каждые 30 минут по 2 поста).
*   `/set_mode @link [0 или 1]` — Сменить режим (0-Forward, 1-Copy).
*   `/list` — Вывести полную таблицу настроек и таймеров.
*   `/delete @link` — Исключить чат из сетки вещания.
*   `/help` — Вызов интерактивной справки.

### 6.3. Ручное вещание (Priority Send)
Команда `/send_ad [ссылка_на_пост]` позволяет мгновенно разослать конкретное рекламное сообщение по всей базе чатов, игнорируя текущие таймеры.

---

## 7. ТЕХНИЧЕСКОЕ ОБСЛУЖИВАНИЕ И БЕЗОПАСНОСТЬ
*   **Синхронизация времени:** Система автоматически использует стандарт UTC для предотвращения конфликтов при работе на серверах в разных часовых поясах.
*   **Логирование:** Все операции (успешные отправки, ошибки, смены статусов) фиксируются в консоли в реальном времени.

---
**Разработчик:** BelGray (Даниил)

**Поддержка:** В рамках текущего контракта на Kwork.